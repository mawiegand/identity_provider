<p id="notice"><%= notice %></p>

<p>
  <b>Identity:</b>
  <%= @install_tracking_install_user.identity_id %>
</p>

<p>
  <b>Install:</b>
  <%= @install_tracking_install_user.install_id %>
</p>

<p>
  <b>Last use at:</b>
  <%= @install_tracking_install_user.last_use_at %>
</p>

<p>
  <b>First use at:</b>
  <%= @install_tracking_install_user.first_use_at %>
</p>

<p>
  <b>Sign in count:</b>
  <%= @install_tracking_install_user.sign_in_count %>
</p>

<p>
  <b>Last ip address:</b>
  <%= @install_tracking_install_user.last_ip_address %>
</p>

<p>
  <b>Last latitude:</b>
  <%= @install_tracking_install_user.last_latitude %>
</p>

<p>
  <b>Last longitude:</b>
  <%= @install_tracking_install_user.last_longitude %>
</p>

<p>
  <b>Last postion at:</b>
  <%= @install_tracking_install_user.last_postion_at %>
</p>


<%= link_to 'Edit', edit_install_tracking_install_user_path(@install_tracking_install_user) %> |
<%= link_to 'Back', install_tracking_install_users_path %>
