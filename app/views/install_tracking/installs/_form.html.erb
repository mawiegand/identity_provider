<%= form_for(@install_tracking_install) do |f| %>
  <% if @install_tracking_install.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@install_tracking_install.errors.count, "error") %> prohibited this install_tracking_install from being saved:</h2>

      <ul>
      <% @install_tracking_install.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :release_id %><br />
    <%= f.number_field :release_id %>
  </div>
  <div class="field">
    <%= f.label :device_id %><br />
    <%= f.number_field :device_id %>
  </div>
  <div class="field">
    <%= f.label :app_token %><br />
    <%= f.text_field :app_token %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
