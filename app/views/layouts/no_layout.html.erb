<!DOCTYPE html>
<html>
<head>
  <title>5dentity | <%= @title %></title>
  <%= csrf_meta_tags %>
</head>
<body>

  <% flash.each do |key, value| %>
    <div class="flash <%= key %>"><%= value %></div>
  <% end %>
  
  <%= yield %>  

</body>
</html>
