<%= form_for(@resource_signup_gift) do |f| %>
  <% if @resource_signup_gift.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resource_signup_gift.errors.count, "error") %> prohibited this resource_signup_gift from being saved:</h2>

      <ul>
      <% @resource_signup_gift.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :identity_id %><br />
    <%= f.number_field :identity_id %>
  </div>
  <div class="field">
    <%= f.label :client_id %><br />
    <%= f.number_field :client_id %>
  </div>
  <div class="field">
    <%= f.label :key_id %><br />
    <%= f.number_field :key_id %>
  </div>
  <div class="field">
    <%= f.label :data %><br />
    <%= f.text_area :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
