<div class="signin-panel-wrapper">
<div class="signin-heading"><%= I18n.translate 'sessions.signin.heading' %></div> 
<div class="signin-panel">

<%= form_for(:session, :url => sessions_path, :html => { :id => 'loginForm'}) do |f| %>
  <div class="field">
    <%= f.label I18n.translate 'sessions.signin.form.login' %><br/>
    <%= f.text_field :login %>
  </div>
  <div class="field">
    <%= f.label I18n.translate 'sessions.signin.form.password' %><br/>
    <%= f.password_field :password %>
  </div>
  <div class="actions">
    <%= f.submit I18n.translate('sessions.signin.form.submit_button'), :class => "button-on-white" %>
  </div>
<% end %>
</div>

<div class="register-link"><%= I18n.translate 'sessions.signin.new_user.question' %>
   <%= link_to I18n.translate('sessions.signin.new_user.link'), new_identity_path %>  </div>
 </div>
  
<script>
/*
$(document).ready(function()
  { 
    $('#loginForm').submit(function()
    { 
      var username    = $('#session_email').val();
      var password    = $('#session_password').val();

      $.ajax(
        {
          'password' : password,
          'username' : username,
          'url'      : '/identities',
          'type'     : 'GET',
          'success'  : function(){ window.location = '<%= identities_path %>'; },
          'error'    : function(){ alert('Bad Login Details');},
        }
      );

      return false;
    });
  });*/
</script>